[phases.setup]
nixPkgs = ["nodejs_22"]
cmds = ["npm install -g pnpm@9"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build:railway"]

[start]
cmd = "PORT=$PORT node --enable-source-maps ./artifacts/api-server/dist/index.mjs"
